import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
	palette: {
		primary: {
			main: '#2E4057',
		},
		action: {
			main: '#FE5F55',
		},
		// action: {
		// 	main: '#f18f01',
		// },
	},
});

export default theme;
